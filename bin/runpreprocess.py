#!/usr/bin/python
import os
import shutil

import preprocess
from common import daemon



def runAll(args):
	#print info message
	
	#make sure filepaths are in expected format
	
	#get list of fastq files to process (depending on args.samples)
	
	#use the daemon to and preprocessing code to trim all fastq files with parallel processing
	
	#either delete or move full length files to a new folder (depending on args.remove)
	
	#print completion message


  return 0
